<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/partials.css">
  
  <title>Edit Food Item</title>
</head>

<body>
  <header class="page-header">
  <h1>Edit Pantry Item: <%= foodItem.name %></h1>
  <a class="btn" href="/users/<%= user._id %>/foods">Back to Pantry</a>
  </header>
  <form action="/users/<%= user._id %>/foods/<%= foodItem._id %>?_method=PUT" method="POST">
    <label for="name">Food Name:</label>
    <input type="text" id="name" name="name" value="<%= foodItem.name %>" required />

    <label for="description">Description:</label>
    <textarea id="description" name="description"><%= foodItem.description %></textarea>

    <button class="btn" type="submit">Update Item</button>
  </form>
</body>

</html>